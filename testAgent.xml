<plominodatabase id="iol_praticaweb">
  <design>
    <element id="testAgent" title="testAgent" type="PlominoAgent">
      <id type="Products.Archetypes.Field.StringField">testAgent</id>
      <Content type="Products.Archetypes.Field.TextField"><![CDATA[docId = args[0]
data = args[1]
db=context.getParentDatabase()

context.plone_log('x<zx<zx<zx<zx')
context.plone_log(args)

context.plone_log(db.resources.config_map().get("mapSettings")["center"])
return


if data['esito']=='OK' and data['azioni']:
    doc=db.getDocument(docId)   
    azioni=data['azioni'].split(',')
    for azione in azioni:
        context.plone_log(azione)
        if doc.isActionSupported(azione):
            doc.doAction(azione)]]></Content>
      <RunAs type="Products.Archetypes.Field.StringField">OWNER</RunAs>
    </element>
  </design>
</plominodatabase>
